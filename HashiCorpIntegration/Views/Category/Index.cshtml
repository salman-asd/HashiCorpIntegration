@model IEnumerable<HashiCorpIntegration.Entities.Category>
@{
    ViewData["Title"] = "Categories";
}
<div class="container mx-auto px-4 py-6">
    <h1 class="text-2xl font-bold mb-4">Categories</h1>
    <a asp-action="Create" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600 mb-4 inline-block">Create New Category</a>
    <table class="min-w-full bg-white border border-gray-200 rounded shadow">
        <thead>
            <tr>
                <th class="py-2 px-4 border-b">Name</th>
                <th class="py-2 px-4 border-b">Actions</th>
            </tr>
        </thead>
        <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td class="py-2 px-4 border-b">@item.Name</td>
                <td class="py-2 px-4 border-b">
                    <a asp-action="Edit" asp-route-id="@item.Id" class="text-blue-500 hover:underline mr-2">Edit</a>
                    <a asp-action="Details" asp-route-id="@item.Id" class="text-green-500 hover:underline mr-2">Details</a>
                    <a asp-action="Delete" asp-route-id="@item.Id" class="text-red-500 hover:underline">Delete</a>
                </td>
            </tr>
        }
        </tbody>
    </table>
</div>